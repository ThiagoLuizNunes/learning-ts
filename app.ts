let message: string = 'Help me Obi-Wan Kenogi, you are my only hop!';

console.log(message);

let episode: number = 0;
episode = episode + 1;


let isEnoughToBeatMF = function (parsecs: number): boolean {
  return parsecs < 12;
}

let distance = 14;
console.log(`Is ${distance} parsecs enough to beat Millenium Falcon?`);
